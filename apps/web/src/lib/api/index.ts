/**
 * API Module Exports
 *
 * All data fetching goes through the InsForge SDK directly.
 * Auth is handled exclusively by @insforge/nextjs (InsforgeMiddleware + SignInButton).
 */
export { USE_MOCK, DEV_LOGIN, DEFAULT_PAGE_SIZE } from "./config";
