# ============================================================
# Nezuko Bot - Environment Configuration
# ============================================================
# This file configures the Telegram bot worker process.
#
# TWO WAYS TO CONFIGURE:
# 
# Option A: Standalone Mode (BOT_TOKEN in this file)
#   - Set BOT_TOKEN below
#   - Bot runs independently
#
# Option B: Dashboard Mode (Recommended)
#   - Leave BOT_TOKEN empty
#   - Add bots via Dashboard UI
#   - Tokens are stored encrypted in database
#   - Start bot with: python -m apps.bot.main --from-db
# ============================================================

# ==========================================================
# BOT TOKEN (Optional in Dashboard Mode)
# ==========================================================
# If set, bot uses this token directly
# If empty, bot reads active bots from database
BOT_TOKEN=

# ==========================================================
# DATABASE
# ==========================================================
# PostgreSQL (Production/Docker) - RECOMMENDED
# Must match the API's valid DATABASE_URL
DATABASE_URL=postgresql+asyncpg://nezuko:nezuko123@localhost:5432/nezuko

# SQLite (Development fallback)
# DATABASE_URL=sqlite+aiosqlite:///../../storage/data/nezuko.db

# ==========================================================
# ENVIRONMENT
# ==========================================================
ENVIRONMENT=development

# ==========================================================
# ENCRYPTION (Required for Dashboard Mode)
# ==========================================================
# Must match the API's ENCRYPTION_KEY for token decryption
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# ==========================================================
# REDIS (Optional but recommended)
# ==========================================================
REDIS_URL=redis://localhost:6379/0

# ==========================================================
# WEBHOOK (Production only)
# ==========================================================
# Leave empty for development (uses polling)
WEBHOOK_URL=
WEBHOOK_SECRET=
PORT=8443
