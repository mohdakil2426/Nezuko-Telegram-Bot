# ============================================================
# Nezuko API - Environment Configuration
# ============================================================
# This file configures the FastAPI backend that powers the dashboard.
#
# SETUP STEPS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Get a login bot token from @BotFather
# 3. Get your Telegram ID from @userinfobot
# 4. Fill in the values below
# ============================================================

# ==========================================================
# TELEGRAM LOGIN (REQUIRED)
# ==========================================================
# Create a simple bot with @BotFather just for dashboard login
# This bot ONLY handles authentication, not enforcement
LOGIN_BOT_TOKEN=

# Your Telegram user ID - ONLY this ID can access the dashboard
# Message @userinfobot on Telegram to get your ID
BOT_OWNER_TELEGRAM_ID=

# ==========================================================
# SECURITY (REQUIRED)
# ==========================================================
# Encryption key for storing bot tokens securely
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Session duration in hours (default: 24)
SESSION_EXPIRY_HOURS=24

# Secret key for cookies/JWT
SECRET_KEY=change_this_in_production

# ==========================================================
# DATABASE
# ==========================================================
# SQLite for local development (simple, no setup)
# DATABASE_URL=sqlite+aiosqlite:///../../storage/data/nezuko.db

# PostgreSQL (Production/Docker) - RECOMMENDED
# docker run -d --name nezuko-postgres -e POSTGRES_USER=nezuko -e POSTGRES_PASSWORD=nezuko123 -e POSTGRES_DB=nezuko -p 5432:5432 postgres:17-alpine
DATABASE_URL=postgresql+asyncpg://nezuko:nezuko123@localhost:5432/nezuko

# Cloud PostgreSQL (Supabase/Neon)
# DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# ==========================================================
# REDIS (Optional)
# ==========================================================
REDIS_URL=redis://localhost:6379/0
REDIS_PASSWORD=

# ==========================================================
# ENVIRONMENT
# ==========================================================
ENVIRONMENT=development
LOG_LEVEL=INFO

# ==========================================================
# API SERVER
# ==========================================================
API_BASE_URL=http://localhost:8080
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
API_DEBUG=true
MOCK_AUTH=false

# ==========================================================
# OBSERVABILITY (Optional)
# ==========================================================
SENTRY_DSN=
METRICS_PORT=9090
